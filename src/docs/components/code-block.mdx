---
title: Code Block
description: The main UI components to create a custom code block.
category: UI
---

<ComponentPreview component="code-block-structure" />

This is the main structure of the `CodeBlock` component, which includes a global wrapper, header and content area:

```tsx
<CodeBlock>
  <CodeBlockHeader />
  <CodeBlockContent>
    {/* Your syntax highlighted code goes here. */}
  </CodeBlockContent>
</CodeBlock>
```

## Installation

1. Create a custom `languageSvgs` utility to map language names to SVG icons:

Install the optional [`@react-symbols/icons`](https://react-symbols.vercel.app/) package for SVG icons:

```bash
npm i @react-symbols/icons
```

Then, create the utility:

<ShowSource component="language-svgs" />

2. Create the `code-block.tsx` component:

<ShowSource component="code-block-structure" />

## Usage

```tsx
import { CodeBlock, CodeBlockHeader, CodeBlockContent } from "@/components/code-block/code-block";

<CodeBlock>
  <CodeBlockHeader title="Hello" language="bash" />
  <CodeBlockContent>
    {/* Your syntax highlighted code goes here. */}
  </CodeBlockContent>
</CodeBlock>
```

## Props

<ShowProps component="code-block-structure" />