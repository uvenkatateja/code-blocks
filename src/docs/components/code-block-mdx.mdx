---
title: Code Block MDX
description: How to create a MDX code block component with Copy Button.
category: UI
---

<Showcase subtitle="Code Block MDX + Copy Button + Shiki Highlighter">

```tsx
import type { ComponentProps } from "react";

const MyComponent = (props: ComponentProps<"div">) => {
  return <div {...props}>Hello, World!</div>;
};

export default MyComponent;
```

</Showcase>

## Installation

Before creating the MDX component, make sure you have the basic component structure:

<DocCard folder="components" document="code-block" />

1. Install the following dependencies:

```bash
npm i @types/mdx -D
```

2. Create the Copy Button component:

<DocCard folder="components" document="copy-button" />

3. Create a `react-to-text` utility to get plain text from `pre` elements:

<ShowSource component="react-to-text" />


## Highlighter Setup

Setup your MDX highlighter:

<MDXCodeBlockSource>
  <TabsContent value="shiki">

1. Setup ``data-title`` & ``data-language``:

<DocCard folder="shiki" document="add-properties" anchor="addtitleproperty" />

2. Setup Shiki MDX integration:

<DocCard folder="shiki" document="highlighter" anchor="mdx-integration" />

3. Create your ``PreShikiComponent`` component:

<ShowSource component="code-block-mdx-shiki" />

4. Usage:

In your MDX Components object, add the new `PreShikiComponent` component:

```ts {3,6}
import type { MDXComponents as MDXComponentsType } from "mdx/types";

import { PreShikiComponent } from "@/components/code-block/pre-shiki";

const MDXComponents: MDXComponentsType = {
  ...PreShikiComponent,
};
```

  </TabsContent>
  <TabsContent value="sugar-high">
    <ShowSource component="code-block-mdx-sugar-high" />
  </TabsContent>
</MDXCodeBlockSource>
