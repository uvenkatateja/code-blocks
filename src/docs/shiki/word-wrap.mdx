---
title: Word Wrap
description: How to enable word wrap in code blocks using Shiki.
category: Shiki
---

## Installation

1. Copy the `wordWrapContent` transformer into your project:

<ShowSource component="shiki-word-wrap" />

2. Import the transformer in your `rehypeShiki` plugin:

```ts title="Rehype Shiki Options"
import { wordWrapContent } from "@/utils/shiki/transformers/show-line-numbers";

const rehypeShikiOptions: RehypeShikiCoreOptions = {
  //...
  transformers: [wordWrapContent()],
};

export { rehypeShikiOptions };
```

Then, automatically long lines in your code blocks will wrap based on the width of the container.